# League of Legends — фильтр по способностям (Frontend, Data Dragon)

Этот проект — статичная HTML+CSS+JS страница, которая загружает данные чемпионов из Riot Data Dragon и даёт удобные фильтры по способностям и параметрам.

## 🚀 Быстрый старт

### Вариант 1: Онлайн режим (CDN)
1. Откройте файл `index.html` в браузере
2. Данные загрузятся автоматически из CDN

### Вариант 2: Офлайн режим (Локальные файлы) ⭐ Рекомендуется

1. Скачайте все ресурсы:
   ```powershell
   .\download_assets.ps1
   ```

2. Запустите локальный сервер:
   ```powershell
   python -m http.server 8000
   ```

3. Откройте: http://localhost:8000

**Подробная инструкция:** см. [SETUP_LOCAL.md](SETUP_LOCAL.md)

## Как запустить

- Откройте файл `index.html` в браузере (Chrome/Edge). Никакого сервера не требуется.
- При первом открытии страница определяет актуальную версию Data Dragon и кэширует разобранные данные чемпионов в `sessionStorage` (ключ меняется при обновлениях парсинга).

## 🌍 Локализация

Приложение поддерживает полную локализацию:
- 🇷🇺 Русский язык (по умолчанию)
- 🇬🇧 Английский язык

Переключатель языка находится справа в строке поиска.

## Источники данных

- Основной источник — Data Dragon (официальные статические данные Riot):
  - Базовый домен: `https://ddragon.leagueoflegends.com`
  - Список доступных версий:  
    `https://ddragon.leagueoflegends.com/api/versions.json`
  - Список доступных локалей:  
    `https://ddragon.leagueoflegends.com/cdn/languages.json`
  - Индекс чемпионов (по версии и локали):  
    `https://ddragon.leagueoflegends.com/cdn/{version}/data/{locale}/champion.json`  
    Пример: `.../cdn/15.21.1/data/en_US/champion.json`
  - Детали одного чемпиона:  
    `https://ddragon.leagueoflegends.com/cdn/{version}/data/{locale}/champion/{ChampionId}.json`  
    Пример: `.../cdn/15.21.1/data/ru_RU/champion/Brand.json`
  - Медиа (по версии):
    - Иконка чемпиона: `.../cdn/{version}/img/champion/{image.full}` (например, `Aatrox.png`)
    - Иконка умения: `.../cdn/{version}/img/spell/{image.full}` (например, `AatroxQ.png`)
    - Иконка пассивки: `.../cdn/{version}/img/passive/{image.full}`
  - Медиа (без привязки к версии, “арт”):
    - Сплэш: `.../cdn/img/champion/splash/{ChampionId}_{skinNumber}.jpg`
    - Превью: `.../cdn/img/champion/tiles/{ChampionId}_{skinNumber}.jpg`

- Дополнительный источник (при необходимости уточнений параметров) — Community Dragon (неофициальное, от сообщества):
  - Базовый домен: `https://raw.communitydragon.org`
  - Агрегированные чемпионы:  
    `.../latest/plugins/rcp-be-lol-game-data/global/{locale}/v1/champions/{numericId}.json`  
    Пример (Brand, id=63):  
    `https://raw.communitydragon.org/latest/plugins/rcp-be-lol-game-data/global/en_us/v1/champions/63.json`
  - Отдельные файлы умений (низкоуровневые, с `mDataValues`):  
    `.../latest/game/data/characters/{lowercaseName}/spells/{lowercaseName}{q|w|e|r}.spell.bin.json`  
    Пример:  
    `https://raw.communitydragon.org/latest/game/data/characters/brand/spells/brandw.spell.bin.json`

В проекте фактически используются только Data Dragon (обязательные): версии, списки и подробные карточки чемпионов, а также картинки. Community Dragon упомянут как альтернативный источник конкретных параметров (например, если в DDragon отсутствует `datavalues.shredduration` или его аналог).

Локали, которые использует проект:

- `en_US` — для анализа текста (поиск “magic/physical damage” и т.д., английские формулировки стабильнее).
- `ru_RU` — для отображения имён и описаний в UI (модальное окно по клику).

## Файлы проекта

- `index.html` — разметка и подключение.
- `styles.css` — стили.
- `script.js` — загрузка и кэш, парсинг способностей, фильтры, отрисовка, модальное окно.

## Структура данных (обзор)

### Индекс чемпионов: `champion.json`

```json
{
  "type": "champion",
  "format": "standAloneComplex",
  "version": "15.21.1",
  "data": {
    "Aatrox": {
      "version": "15.21.1",
      "id": "Aatrox",
      "key": "266",
      "name": "Aatrox",
      "title": "the Darkin Blade",
      "image": { "full": "Aatrox.png", "...": "..." },
      "tags": ["Fighter", "Tank"],
      "stats": { "attackrange": 175, "...": "..." }
    },
    "...": {}
  }
}
```

Важные поля, которые использует проект:
- `data[champ].image.full` — иконка чемпиона.
- `data[champ].tags` — роли чемпиона (Fighter, Tank, Mage, Marksman, Assassin, Support).
- `data[champ].stats.attackrange` — дальность автоатаки (для бегунка “мин. дальность автоатаки”).  

### Детали чемпиона: `{ChampionId}.json`

```json
{
  "data": {
    "Aatrox": {
      "id": "Aatrox",
      "name": "Aatrox",
      "title": "the Darkin Blade",
      "spells": [
        {
          "id": "AatroxQ",
          "name": "The Darkin Blade",
          "tooltip": "Deals {{ totaldamage }} physical damage ...",
          "description": "Full description ...",
          "cooldownBurn": "14/12/10/8/6",
          "costBurn": "0",
          "range": [650, 650, 650, 650, 650],            // иногда число | массив | строка
          "rangeBurn": "650",
          "effect": [null, [10, 20, 30, 40, 50], ...],   // е1, е2, ... (числа по уровням)
          "effectBurn": [null, "10/20/30/40/50", ...],   // е1, е2, ... (строки)
          "datavalues": { "shredduration": 1.75, "...": 0 },
          "vars": [
            { "key": "totaldamage", "coeff": [30, 60, 90, 120, 150], "link": "attackdamage" },
            { "key": "slowamount", "coeff": 0.45, "link": "unknown" }
          ],
          "image": { "full": "AatroxQ.png" }
        },
        "..."
      ],
      "passive": {
        "name": "Deathbringer Stance",
        "description": "Aatrox's next basic attack deals ...",
        "image": { "full": "Aatrox_Passive.png" }
      }
    }
  }
}
```

Ключевые поля у `spells[]`:
- `name`, `tooltip`, `description` — локализованный текст. В `tooltip/description` встречаются плейсхолдеры вида `{{ key }}`.
- `cooldownBurn`, `costBurn`, `rangeBurn` — значения по уровням в текстовом формате “a/b/c/...”.  
  Альтернатива `range` — число или массив чисел.
- `effect`/`effectBurn` — массивы, где `effect[i]` соответствует плейсхолдеру `{{ e{i} }}`.
- `datavalues` — именованные параметры (например, `shredduration`, `shieldduration` и т.п.).
- `vars[]` — связывает ключ плейсхолдера с коэфф-ами и источником (например, `totaldamage`, `rampdamagecalc`, `slowamount`):
  - `key` — имя плейсхолдера (без фигурных скобок).
  - `coeff` — число или массив чисел (часто значения по уровням).
  - `link` — от чего скейлится (например, `attackdamage`, `spelldamage`, `health`, `maxhealth`, `bonushealth`).

## Что делает приложение

- Загружает список чемпионов и затем детали каждого чемпиона в двух локалях: `en_US` и `ru_RU`.
- Для каждого чемпиона формирует агрегированный объект:
  - `attackRange` — дальность автоатаки.
  - `classTags` — роли (tank/fighter/mage/marksman/assassin/support).
  - `tags` — эвристические метки по тексту умений (mobility, stun, slow, root, knockup, silence, stealth, shield, heal).
  - `damageTypes` — набор, определяемый из английского описания (“magic damage”/“physical damage”).
  - `scalesWithOwnHealth` — логика проверки по `vars.link` (наличие `health|maxhealth|bonushealth`).
  - `spellRanges.{Q|W|E|R}` — максимальная дальность умения (берётся из `range`/`rangeBurn`, корректно обрабатываются “Global/Infinite”).
  - `ru` — скомпилированные описания умений на русском с автоподстановкой плейсхолдеров.

### Автоподстановка плейсхолдеров

Перед показом описаний в модальном окне тексты проходят замену `{{ key }}` на значения. Система работает в три этапа:

**1. Поиск точных данных из Data Dragon:**
- `spells[i].datavalues` (en, затем ru как fallback)
- `spells[i].effectBurn` → плейсхолдеры `{{ e1 }}`, `{{ e2 }}` …
- Часто используемые агрегаты: `cooldownBurn` (`cooldown`/`cooldownburn`), `costBurn` (`cost/costburn`), `rangeBurn` (`range/rangeburn`)
- `spells[i].vars[]` — ключи вида `totaldamage`, `rampdamagecalc`, `shieldduration`, `slowamount` и т.д. (число или "a/b/c" при массиве по уровням).

**2. Fallback на человекочитаемые описания:**
Если точных данных нет в Data Dragon, система использует встроенный словарь с понятными описаниями для ~200+ типичных плейсхолдеров LoL. Примеры замен:
- **Урон:** `{{ missinghealthpercent }}` → "% от недостающего здоровья", `{{ qultdamagebeam }}` → "урон луча", `{{ wulttotaldamage }}` → "общий урон"
- **Контроль:** `{{ slowamount }}` → "сила замедления", `{{ stunduration }}` → "длительность оглушения", `{{ knockupduration }}` → "длительность подброса"
- **Защита:** `{{ shieldduration }}` → "длительность щита", `{{ armor }}` → "броня", `{{ magicresist }}` → "магическая защита"
- **Лечение:** `{{ healingcalc }}` → "лечение", `{{ lifesteal }}` → "вампиризм", `{{ omnivamp }}` → "все-вампиризм"
- **Скейлы:** `{{ adscaling }}` → "скейл от силы атаки", `{{ healthscaling }}` → "скейл от здоровья"
- **Прочее:** `{{ spellmodifierdescriptionappend }}` → удаляется (пустая строка), `{{ maxcharges }}` → "макс. количество зарядов"

**3. Сохранение незаполненных плейсхолдеров:**
Если для конкретного `{{ key }}` не найдено ни данных, ни fallback-описания, плейсхолдер остаётся в тексте как `{{ key }}` — это сигнал, что параметр полностью отсутствует и требует ручной проверки.

**Примечание:** В современных версиях Data Dragon (начиная с ~15.x) массивы `vars` и `datavalues` пустые для большинства чемпионов, поэтому fallback-словарь играет критическую роль в создании читаемых описаний.

### Фильтры

- По эффектам: прыжок/рывок, стан, замедление, root, подброс/отброс, молчание, невидимость, щит, лечение (эвристики по английскому тексту).
- По ролям: Танк, Воин, Маг, Стрелок, Убийца, Поддержка.
- По типу урона у способностей: Физический, Магический.
- По скейлу от собственного здоровья:
  - Сначала проверяется `vars.link` (для старых версий Data Dragon)
  - Затем ищутся плейсхолдеры здоровья в тексте описаний (`maxhealth`, `bonushealth`, `missinghealthpercent`, и т.д.)
- По дальности:
  - автоатаки;
  - минимальная дальность для каждой из способностей Q/W/E/R (берётся максимум из `range`/`rangeBurn`). “Global/Infinite” учитывается как очень большая дальность.

### Модальное окно (по клику на иконке чемпиона)

- Заголовок: имя, титул, роли, типы урона, дальность автоатаки, признак "Скейл от здоровья".
- Способности: пассивка, Q/W/E/R с иконками, названиями и автоподставленными значениями в описаниях. Рядом с названием — подсказка о дальности (например, "Дальность: 850" или "Дальность: глобальная").
- Окно без скроллов: содержимое масштабируется под размер экрана; ширина модалки — 1000px (не больше 96vw), высота — по содержимому.

## Ограничения и примечания

- Data Dragon — статический снимок данных. Некоторые именованные параметры (`datavalues`) встречаются не у всех умений и не на всех патчах.
- Если нужного параметра нет в DDragon, его часто можно найти в Community Dragon (не используется проектом по умолчанию, но легко добавить):
  - Агрегированные чемпионы:  
    `https://raw.communitydragon.org/latest/plugins/rcp-be-lol-game-data/global/en_us/v1/champions/{championId}.json`
  - Файлы умений (более “низкоуровневые”):  
    `https://raw.communitydragon.org/latest/game/data/characters/{lowercaseName}/spells/{lowercaseName}{q|w|e|r}.spell.bin.json`
  - В CDragon ищите поля наподобие `mDataValues` → `ShredDuration`, `ShieldDuration`, `TotalDamage` и т.п.

## Как менять/расширять

- **Локализация UI** — через `index.html`/`styles.css`; язык данных — константы `LOCALE` и `RU_LOCALE` в `script.js`.

- **Новые фильтры:**
  1) В `index.html` — разметка чекбоксов/ползунков;
  2) В `script.js` — чтение значений в `readFiltersFromUI()` и условия в `championMatchesFilters()`.

- **Детекция типов урона** — расширить парсер в `extractDamageTypesFromDetailEN` (например, добавить "true damage").

- **Добавление новых плейсхолдеров в словарь:**
  В функции `fillPlaceholdersInText()` (script.js, строка ~113) есть объект `fallbackDescriptions`. Чтобы добавить новый плейсхолдер:
  ```javascript
  const fallbackDescriptions = {
    // ... существующие ...
    "newplaceholder": "понятное описание",
    "anotherkey": "другое описание"
  };
  ```
  Плейсхолдеры, которые нужно просто удалить (без замены), задаются пустой строкой: `"keytoremove": ""`.

- **Интеграция Community Dragon (опционально):**
  Добавить загрузку данных из CDragon API параллельно с Data Dragon и использовать `mDataValues` для заполнения пробелов. Потребуется:
  1) Запрос к `https://raw.communitydragon.org/latest/plugins/rcp-be-lol-game-data/global/en_us/v1/champions/{numericId}.json`
  2) Парсинг полей `mDataValues` (например, `ShredDuration`, `TotalDamage`)
  3) Добавление их в словарь `dict` внутри `fillPlaceholdersInText()` с нормализацией ключей к lowercase.

## Лицензии и права

Неофициальный инструмент. Все права на материалы принадлежат Riot Games. Данные берутся из публичных источников Riot (Data Dragon). Имена и изображения чемпионов/умений принадлежат соответствующим владельцам.


